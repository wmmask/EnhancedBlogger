@model IEnumerable<EnhancedBlogger.Models.BlogSubTheme>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BlogSubThemetitle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SubThemeCreateDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BlogTheme)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BlogSubThemetitle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SubThemeCreateDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BlogTheme.BlogThemetitle)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BlogSubThemeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BlogSubThemeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BlogSubThemeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
