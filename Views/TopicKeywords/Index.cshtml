@model IEnumerable<EnhancedBlogger.Models.TopicKeyword>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TopicKeywordName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TopicKeywordRemark)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.KeywordCreateDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TopicItem)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TopicKeywordName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TopicKeywordRemark)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.KeywordCreateDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TopicItem.TopicItemId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TopicKeywordId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TopicKeywordId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TopicKeywordId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
